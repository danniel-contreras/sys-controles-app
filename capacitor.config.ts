import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.syscontroles.facela.app',
  appName: 'sys-controles-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
